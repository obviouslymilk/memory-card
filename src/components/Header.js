import logo from '../assets/logo.png';

function Header() {
    return (
        <div id='Header'>
            <img src={logo} alt="Hearthstone Memory Card" />
        </div>
    );
}

export default Header;